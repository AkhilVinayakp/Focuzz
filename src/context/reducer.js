import { START_TIMER, 
        STOP_TIMER,

} from "./action.types";

const reducer = (state, action) => {
    switch(action.type){
        case START_TIMER:
            break;
        case STOP_TIMER:
            break;
    }
}
export default reducer;